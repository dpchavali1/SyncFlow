{
  "database": {
    "rules": "database.rules.json"
  },
  "storage": {
    "rules": "storage.rules"
  },
  "functions": {
    "source": "functions",
    "codebase": "default",
    "predeploy": [
      "echo 'Skipping lint for now - functions are syntactically correct'"
    ]
  }
}
