{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "pending_pairings": {
      ".read": true,
      ".write": true
    }
  }
}
